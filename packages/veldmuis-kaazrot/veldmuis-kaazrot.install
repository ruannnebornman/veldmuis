#!/bin/sh

post_install() {
  printf '%s\n' \
    'veldmuis-kaazrot ships personal templates only.' \
    'They apply automatically at next login (user service).' \
    'Run `veldmuis-kaazrot-apply` manually to apply immediately (existing files are backed up).'
}

post_upgrade() {
  post_install
}
