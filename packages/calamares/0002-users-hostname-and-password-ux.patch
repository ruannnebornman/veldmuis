diff --git a/src/modules/users/Config.cpp b/src/modules/users/Config.cpp
index be75477..0f83db6 100644
--- a/src/modules/users/Config.cpp
+++ b/src/modules/users/Config.cpp
@@ -593,6 +593,11 @@ Config::setUserPasswordSecondary( const QString& s )
 Config::PasswordStatus
 Config::passwordStatus( const QString& pw1, const QString& pw2 ) const
 {
+    if ( pw1.isEmpty() || pw2.isEmpty() )
+    {
+        return qMakePair( PasswordValidity::Invalid, tr( "Enter the same password twice." ) );
+    }
+
     if ( pw1 != pw2 )
     {
         return qMakePair( PasswordValidity::Invalid, tr( "Your passwords do not match!" ) );
@@ -609,7 +614,7 @@ Config::passwordStatus( const QString& pw1, const QString& pw2 ) const
         }
     }
 
-    return qMakePair( PasswordValidity::Valid, tr( "OK!" ) );
+    return qMakePair( PasswordValidity::Valid, QString() );
 }
 
 Config::PasswordStatus
@@ -1041,6 +1046,7 @@ Config::setConfigurationMap( const QVariantMap& configurationMap )
     checkReady();
 
     ApplyPresets( *this, configurationMap ) << "fullName"
+                                            << "hostname"
                                             << "loginName";
 }
 
diff --git a/src/modules/users/UsersPage.cpp b/src/modules/users/UsersPage.cpp
index da23532..effe1fb 100644
--- a/src/modules/users/UsersPage.cpp
+++ b/src/modules/users/UsersPage.cpp
@@ -37,16 +37,28 @@
 static inline void
 labelError( QLabel* pix, QLabel* label, Calamares::ImageType icon, const QString& message )
 {
+    const auto style = icon == Calamares::StatusWarning ? QStringLiteral( "color: #e7be63;" )
+                                                        : QStringLiteral( "color: #ff8f6b;" );
     label->setText( message );
-    pix->setPixmap( Calamares::defaultPixmap( icon, Calamares::Original, label->size() ) );
+    label->setStyleSheet( style );
+    pix->clear();
+    pix->hide();
+}
+
+static inline void
+clearStatus( QLabel* pix, QLabel* label )
+{
+    pix->clear();
+    pix->hide();
+    label->clear();
+    label->setStyleSheet( QString() );
 }
 
 /** @brief Clear error, set happy pixmap on a label to indicate "ok". */
 static inline void
 labelOk( QLabel* pix, QLabel* label )
 {
-    label->clear();
-    pix->setPixmap( Calamares::defaultPixmap( Calamares::StatusOk, Calamares::Original, label->size() ) );
+    clearStatus( pix, label );
 }
 
 /** @brief Sets error or ok on a label depending on @p status and @p value
@@ -63,8 +75,7 @@ labelStatus( QLabel* pix, QLabel* label, const QString& value, const QString& st
         if ( value.isEmpty() )
         {
             // This is different from labelOK() because no checkmark is shown
-            label->clear();
-            pix->clear();
+            clearStatus( pix, label );
         }
         else
         {
@@ -183,6 +194,7 @@ UsersPage::UsersPage( Config* config, QWidget* parent )
 
     ui->textBoxLoginName->setEnabled( m_config->isEditable( "loginName" ) );
     ui->textBoxFullName->setEnabled( m_config->isEditable( "fullName" ) );
+    ui->textBoxHostname->setEnabled( m_config->isEditable( "hostname" ) );
 
     retranslate();
 }
@@ -249,7 +261,7 @@ passwordStatus( QLabel* iconLabel, QLabel* messageLabel, int validity, const QSt
     switch ( validity )
     {
     case Config::PasswordValidity::Valid:
-        labelOk( iconLabel, messageLabel );
+        clearStatus( iconLabel, messageLabel );
         break;
     case Config::PasswordValidity::Weak:
         labelError( iconLabel, messageLabel, Calamares::StatusWarning, message );
diff --git a/src/modules/users/page_usersetup.ui b/src/modules/users/page_usersetup.ui
index 6e6e542..beb94a8 100644
--- a/src/modules/users/page_usersetup.ui
+++ b/src/modules/users/page_usersetup.ui
@@ -346,7 +346,7 @@ SPDX-License-Identifier: GPL-3.0-or-later
         </size>
        </property>
        <property name="toolTip">
-        <string>&lt;small&gt;Enter the same password twice, so that it can be checked for typing errors. A good password will contain a mixture of letters, numbers and punctuation, should be at least eight characters long, and should be changed at regular intervals.&lt;/small&gt;</string>
+        <string>&lt;small&gt;Enter the same password twice so it can be checked for typing errors.&lt;/small&gt;</string>
        </property>
        <property name="echoMode">
         <enum>QLineEdit::Password</enum>
@@ -371,7 +371,7 @@ SPDX-License-Identifier: GPL-3.0-or-later
         </size>
        </property>
        <property name="toolTip">
-        <string>&lt;small&gt;Enter the same password twice, so that it can be checked for typing errors. A good password will contain a mixture of letters, numbers and punctuation, should be at least eight characters long, and should be changed at regular intervals.&lt;/small&gt;</string>
+        <string>&lt;small&gt;Enter the same password twice so it can be checked for typing errors.&lt;/small&gt;</string>
        </property>
        <property name="echoMode">
         <enum>QLineEdit::Password</enum>
