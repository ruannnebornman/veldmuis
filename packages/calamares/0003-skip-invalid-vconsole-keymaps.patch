diff --git a/src/modules/keyboard/SetKeyboardLayoutJob.cpp b/src/modules/keyboard/SetKeyboardLayoutJob.cpp
--- a/src/modules/keyboard/SetKeyboardLayoutJob.cpp
+++ b/src/modules/keyboard/SetKeyboardLayoutJob.cpp
@@ -174,8 +174,9 @@ SetKeyboardLayoutJob::writeVConsoleData( const QString& vconsoleConfPath, const
     }
     if ( keymap.isEmpty() )
     {
-        cDebug() << "Trying to use X11 layout" << m_layout << "as the virtual console layout";
-        keymap = m_layout;
+        cWarning() << "No vconsole keymap mapping for X11 layout" << m_layout << '-' << m_variant
+                   << "; leaving vconsole.conf unchanged.";
+        return true;
     }
 
     QStringList existingLines;
