# Highlights

- Fixed fresh installs failing during bootstrap when the live ISO has no active Arch mirror servers configured.
- The installer now writes a controlled temporary pacman configuration for `pacstrap` and falls back to known Arch mirrors if the live mirrorlist is empty or commented.

# Notes

- Use this ISO instead of `2026.04.1` for laptop install testing.
- The failure happened before graphics packages were installed, so it was not caused by choosing the Intel graphics option.
