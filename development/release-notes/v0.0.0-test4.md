# Highlights

- Full end-to-end prerelease validation for tagged ISO builds.
- GitHub Actions now builds the packages, signs them, publishes the ISO to R2, creates the GitHub Release, and updates the pacman repo on `gh-pages`.
- The website-facing ISO bucket keeps only `latest.iso`, `latest.iso.sha256`, and `latest.manifest.txt`.

# Notes

- This prerelease is meant to confirm the complete hosted release path before laptop installation testing.
- The `downloads.veldmuislinux.org` redirect should resolve to the newest hosted ISO after this run succeeds.
