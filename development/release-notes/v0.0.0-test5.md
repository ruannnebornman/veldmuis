# Highlights

- Full hosted prerelease validation with the GitHub Release flow fixed to use the GitHub CLI.
- ISO publishing, GitHub Release assets, and package repo publishing are all expected in the same run.

# Notes

- This release exists to verify the GitHub Release creation fix after the `test4` failure.
